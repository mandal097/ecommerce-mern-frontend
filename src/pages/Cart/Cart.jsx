import React from 'react';
import styles from './Cart.module.scss';

const Cart = () => {
    return (
        <div className={`${styles.cart}`}>
            <h1>Cart</h1>
        </div>
    )
}

export default Cart